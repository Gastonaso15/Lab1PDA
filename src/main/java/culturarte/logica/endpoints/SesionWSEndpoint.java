package culturarte.logica.endpoints;
/*
import culturarte.servicios.Fabrica;
import culturarte.servicios.interfaces.ISesionController;
import culturarte.servicios.interfaces.web.ISesionControllerWS;
import culturarte.servicios.DTs.DTUsuario;

import jakarta.jws.WebService;

@WebService(endpointInterface = "culturarte.servicios.interfaces.web.ISesionControllerWS")
public class SesionWSEndpoint implements ISesionControllerWS {

    private ISesionController controlador;

    public SesionWSEndpoint() {
        this.controlador = Fabrica.getInstance().getISesionController();
    }

    @Override
    public DTUsuario login(String nick, String password) {
        return controlador.login(nick, password);
    }

    @Override
    public void logout() {
        controlador.logout();
    }

    @Override
    public DTUsuario getUsuarioActual() {
        return controlador.getUsuarioActual();
    }

    @Override
    public boolean isLoggedIn() {
        return controlador.isLoggedIn();
    }
}*/
